[build]
  command = "cd composite_behavioral_modeling && pip install -r ../requirements.txt && python manage.py migrate && python manage.py collectstatic --noinput"
  publish = "composite_behavioral_modeling/staticfiles"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
